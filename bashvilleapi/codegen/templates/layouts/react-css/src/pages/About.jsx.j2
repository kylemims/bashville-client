export const About = () => {
  return (
    <div style={{ minHeight: '100vh', backgroundColor: 'var(--color-background)', padding: '3rem 0' }}>
      <div style={{ maxWidth: '1200px', margin: '0 auto', padding: '0 1rem', textAlign: 'center' }}>
        <h1 style={{ fontSize: '2.5rem', color: 'var(--color-primary)', marginBottom: '1rem' }}>
          About {{ project_title }}
        </h1>
        <p style={{ fontSize: '1.125rem', color: '#666', marginBottom: '2rem' }}>
          CSS Layout - Placeholder Page
        </p>
        <div style={{ 
          backgroundColor: 'white', 
          padding: '2rem', 
          borderRadius: '12px', 
          boxShadow: '0 4px 6px rgba(0, 0, 0, 0.1)',
          maxWidth: '600px',
          margin: '0 auto'
        }}>
          <p style={{ color: '#666', lineHeight: 1.6 }}>
            This is a placeholder for the About page. The full modular structure with detailed documentation will be implemented after the Tailwind version is tested and approved.
          </p>
        </div>
      </div>
    </div>
  )
}
